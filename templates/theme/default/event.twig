{# Custom event pages override the default. les-houches-2026 has its own
   full-page design with its own header/footer (matches the legacy SPA's
   /events/* branch that bypassed the global layout). #}
{% if entry.slug == 'les-houches-2026' %}
    {% include '@theme/event-les-houches.twig' %}
{% else %}
    {% include '@theme/_event-default.twig' %}
{% endif %}
